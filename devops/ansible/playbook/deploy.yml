- name: Installing Nginx on Ubuntu
  hosts: all
  become: yes
      
  tasks:
    - name: Create /var/www/gjpb/blog directory
      file:
        path: /var/www/gjpb/blog  # Change this path to your desired directory
        state: directory
        mode: '0755'

    - name: Upload build to /var/www/gjpb/blog
      copy:
        src: "../../../dist/"
        dest: "/var/www/gjpb/blog"
        mode: 0755
      notify: Restart Nginx

  handlers:
    - name: Restart Nginx
      service:
        name: nginx
        state: restarted